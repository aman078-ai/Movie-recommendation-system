# TMDB API Key (Optional. Generate a free key from https://www.themoviedb.org/settings/api)
# If left empty, the application will fall back to using the curated local dataset (data/movies.csv).
TMDB_API_KEY=
